<!DOCTYPE html>
<html>
  <head>
    <title>
      <% if content_for?(:title) %>
        <%= yield(:title) %>
      <% else %>
        Eligia
      <% end %>
    </title>
    <meta name="description" content="Avec Eligia, résiliez en 10 minutes tous les abonnements d'un proche disparu. Une seule procédure simplifiée pour résiliser les abonnements internet, téléphonie mobile, énergie, médias etc.">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body>
    <%= render 'shared/navbar' %>
    <%= yield %>
    <%= render 'shared/flash' %>
    <%= render 'shared/footer' %>
  </body>
</html>

<%= turbo_stream.update("flash", partial: "shared/flash") %>
<%= turbo_stream.update(@order_document, partial: 'order_accounts/document_input',
                                          locals: {
                                            order_document: @order_document,
                                            order_account: @order_account,
                                            order: @order
                                          }) %>

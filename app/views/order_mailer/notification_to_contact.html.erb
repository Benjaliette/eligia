<div class='welcome'>
  <h1> 🌟🚀 Nouvelle commande !</h1>
</div>


<p>Client : <%= @user.first_name %> <%= @user.last_name %> (email : <%= @user.email %>)</p>

<p>Détail de la commande : </p>

<table class='table'>
  <tr>
    <th>Nom du compte à résilier</th>
    <th>Statut</th>
  </tr>
  <% @order.order_accounts.each do |oa| %>
    <tr>
      <td><%= oa.account.name %></td>
      <% if order_account.account.non_validated? %>
        <td class="non-validated">À valider</td>
      <% else %>
        <td class="<%= order_account.aasm_state%>"><%= order_account.state_to_french %></td>
      <% end %>
    </tr>
  <% end %>
  <tr>
    <td></td>
    <td></td>
  </tr>
  <tr>
    <td class="bold">Nombre de contrats</td>
    <td><%= @order.order_accounts.count %></td>
  </tr>
</table>

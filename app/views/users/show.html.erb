<% content_for :title, "Mon espace - Eligia" %>

<%= render 'shared/navbar' %>
<div class="container">
  <div class="fullscreen">
    <h2 class="no-margin padding-top-five padding-bottom-five">Bienvenue sur votre espace</h2>

    <%= render 'account_infos', user: current_user, resource: current_user %>

    <h3 class="no-margin padding-top-five padding-bottom-two">Vos démarches</h3>

    <% @orders.each do |order| %>
      <%= link_to order_path(order) do  %>
        <%= render "order_dashboard_card", order: order %>
      <% end %>
    <% end %>
  </div>
</div>
